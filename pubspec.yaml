name: connectedx
description: A new app from scratch.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/Logo.png
    - assets/facebook.png
    - assets/apple.png
    - assets/iphone.png
    - assets/google.png
    - assets/icon.png

  